1. Будет выведено число 1, так как внутри функции значение obj.a изменяется на '!', но сам объект obj остается неизменным и сохраняет свои свойства.
2. Будет выведен объект a: 1, b: 2, c: 3, так как при передаче аргумента obj.a в функцию func() он передается по значению, а не по ссылке. Поэтому изменение значения arg внутри функции не влияет на значение свойства a объекта obj.
3. Будет выведен объект a: 1, b: 2, c: 3, так как внутри функции obj перезаписывается на строку '!', но это не влияет на значение переменной obj вне функции.
4. Будет выведен массив [1, 3], так как метод splice() изменяет исходный массив arr, удаляя элемент с индексом 1 (то есть второй элемент) и сдвигая все последующие элементы на одну позицию влево.
5. Будет выведен массив [1, 2, 3], так как метод slice() создает новый массив, содержащий элементы исходного массива arr, начиная с индекса 1 и заканчивая индексом 1 (то есть не включая элемент с индексом 1), но этот новый массив нигде не сохраняется и не используется в коде.
6. Будет выведен массив ['!', 2, 3], так как переменная newArr ссылается на тот же массив, что и arr, и изменение элемента с индексом 0 в newArr приводит к изменению этого элемента в исходном массиве arr.
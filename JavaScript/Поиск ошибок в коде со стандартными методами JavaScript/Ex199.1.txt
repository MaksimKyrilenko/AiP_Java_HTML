№1 - Ошибка, так как метод split() не работает для чисел, нужно сначала преобразовать число в строку.
№2 - Ошибка, так как переменная sum должна быть числом, а не строкой. Нужно использовать оператор += для сложения чисел.
№3 - Ошибка, так как в цикле каждый раз значение переменной sum перезаписывается, а не складывается с предыдущими значениями. Нужно использовать оператор += для сложения чисел.
№4 - Код работает верно и выводит сумму цифр числа.
№5 - Ошибка, так как переменная prod должна быть числом, а не строкой. Начальное значение переменной должно быть 1, а не 0. Нужно использовать оператор *= для умножения чисел.